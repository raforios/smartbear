# Deployment variables for the Analytics microservice.

FUNCTION_NAME=analytics-handler-service
ROLE_NAME=lambda-service-analytics-execution-role
ZIP_FILE=analytics_function.zip
API_NAME=analytics-service
REGION=us-east-1
TIMEOUT=120
# 2048 MB: the affinity engine loads a full sales CSV (~35 MB / 120k rows) and
# runs Apriori + per-PdV scoring — CPU-bound. Lambda scales CPU with memory, so
# 256 MB (~0.15 vCPU) timed out at 120 s; 2048 MB (~1.2 vCPU) brings it under the
# API Gateway 29 s limit. Also speeds up the commercial summary.
MEMORY_SIZE=2048
PROFILE=deploy_ml
S3_ARTIFACTS_BUCKET=lambda-code-microservices
DOCKER_IMAGE_NAME=analytics-lambda-builder
DYNAMODB_TABLE_NAME=analytics_runs
# Clave real de la tabla en AWS. Debe coincidir con lo declarado en
# services/ci/api/create_dynamodb_tables.sh: son dos declaraciones de
# la misma tabla y ya divergieron una vez.
DYNAMODB_PRIMARY_KEY_NAME=id
DYNAMODB_PRIMARY_KEY_TYPE=S
DYNAMODB_TTL_ENABLED=false
